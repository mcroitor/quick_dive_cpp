#include "point.h"
#include <iostream>

int main() {
    Point O(0, 0);
    Point A(2, 3);

    std::cout << "oops!" << std::endl;

    std::cout << "A (" << A.GetX() << "," << A.GetY() << ")" << std::endl;
    return 0;
}