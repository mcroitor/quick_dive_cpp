# Конструкторы и операторы

## Цель

В рамках данной работы студент научится переопределять операторы, а также ознакомится с вариантами определения и использования конструкторов.

Кроме того, данная работа подготовит проект к использованию стандартной библиотеки C++.

## Задание

Задание выполняется на основе предыдущей лабораторной работы. Все изменения делаются в ветке `lab03` репозитория.

Для каждой структуры данных, представляющей элементы игры, определите:

1. конструктор по умолчанию;
2. конструктор копирования;
3. конструктор с параметрами;
4. оператор присваивания;
5. операторы сравнения;
6. операторы ввода и вывода;
7. Для класса, отвечающего за прорисовку игровых объектов, определите базовый абстрактный класс.
8. Выложите изменённый код на GitHub в ветку `lab03` и создайте запрос на слияние в ветку `main`.

## Представление

Для представления результата прикрепите ссылку на репозиторий GitHub к заданию в Moodle.

## Оценивание

- `1 балл` — реализация конструкторов;
- `1 балл` — реализация оператора присваивания;
- `2 балла` — реализация операторов сравнения;
- `2 балла` — реализация операторов ввода и вывода;
- `2 балла` — реализация базового абстрактного класса;
- `2 балла` — защита работы;
- `-1 балл` — за каждый день просрочки сдачи работы;
- `-5 баллов` — за плагиат.

## Рубрика оценивания

| Критерий | Макс. | 0% | 25% | 50% | 75% | 100% |
| -------- | :---: | --- | --- | --- | --- | ---- |
| Конструкторы | 1 | Не реализованы | Реализован конструктор по умолчанию | Реализованы два конструктора | Реализованы все конструкторы с ошибками | Все конструкторы реализованы корректно |
| Оператор присваивания | 1 | Не реализован | Реализован без проверки самоприсваивания | Реализован с ошибками в логике | Реализован корректно для одного класса | Реализован корректно для всех классов |
| Операторы сравнения | 2 | Не реализованы | Реализованы для одного класса | Реализованы для половины классов | Реализованы для большинства классов | Реализованы корректно для всех классов |
| Операторы ввода/вывода | 2 | Не реализованы | Реализованы для одного класса | Реализованы для половины классов | Реализованы для большинства классов | Реализованы корректно для всех классов |
| Абстрактный класс `Painter` | 2 | Не создан | Создан без виртуальных методов | Создан с частичным интерфейсом | `Painter` наследует, но не переопределяет методы | Абстрактный класс и `Painter` реализованы корректно |
| Защита работы | 2 | Не явился / не знаком с кодом | Знаком поверхностно, не может объяснить решения | Объясняет код, допускает существенные ошибки | Понимает работу, допускает незначительные ошибки | Уверенно отвечает на все вопросы |
